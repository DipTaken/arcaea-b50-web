import { PageShell } from "@/app/components/PageShell"

export default async function LeaderboardPage() {
    return (
        <PageShell 
            title="Leaderboard" 
            subtitle="View the top players in the game">
                <></>
        </PageShell>
    )
}