ARCAEA B50 WEB — TODO

Audit backlog (bugs, Tailwind refactor, code quality) lives in report_todo.md.
This file is the running project list.

=== DONE ===
[x] Landing page (/), NavBar with links + profile/login
[x] Browse page: catalog, search, sort, level + difficulty filters
[x] Browse card -> modal with full song info
[x] Real jacket art (Supabase Storage 'jackets' bucket, song_id + jacket_override)
[x] Google auth via Supabase OAuth
[x] Import guest scores to a logged-in account
[x] Score detail modal (ScoreModal -> SongInfo + ScoreInfo)
[x] Real types for the score join (Score / ScoreWithChart)
[x] Commenting pass over app/ and utils/
[x] Server-side validation in addScore; returns { error } instead of throwing
[x] Client-side input validation + error display on the add-score form
[x] Modal takes a width prop (dialog was shrink-wrapping to fit-content)
[x] getPmRating distance, revalidatePath -> /scores, getJacketUrl no longer builds a client
[x] ImportFromBrowser upserts instead of blind-inserting
    NOTE: needs a unique constraint on (user_id, chart_id, created_at) to work.
    Verify it exists in the live DB — see report_todo.md item 4.4.
[x] auth-code-error is a named export (still an empty stub — see report_todo.md 5.3)
[x] Style passes: browse filters, import success message layout
[x] Deleted alt/ (abandoned design experiments)

[x] Perf: one shared modal per grid instead of one per card
    Was ~3600 <dialog> elements + ~1800 forms on /browse. Now 1.
[x] Perf: cap the browse grid at 100 cards with a Load More button
[x] .env.example
    NOTE: .gitignore's `.env*` was swallowing it, so it would never have been
    committed. Added `!.env.example` to un-ignore it. .env.local stays ignored.
[x] Proxy refreshes the session (Step 0 of the anon-auth migration)
    utils/supabase/middleware.ts now awaits supabase.auth.getUser() before
    returning the reassigned supabaseResponse; createClient is async and
    proxy() returns its promise. NOT yet verified at runtime — shorten the JWT
    expiry in the dashboard, idle past it, confirm you stay signed in.
    Still open (cosmetic, from report_todo 1.3): rename createClient ->
    updateSession, since it returns a NextResponse, not a client.

[x] Decided: adding a score from inside a modal leaves the outer modal open.
    close() only closes the inner add-score dialog. Kept as-is.

    Worth remembering from the revalidatePath fix: /scores is dynamic (it reads
    cookies), so revalidate isn't invalidating cached HTML — it's clearing the
    CLIENT router cache, which is what was serving the stale grid after a nav.

=== BUGS (in priority order) ===

[ ] Everything else -> report_todo.md
    Tier 1 there is the urgent set: getShinyPureCount is wrong on ~half of all
    real scores, line-clamp-2 has never clamped, addScore silently discards
    its insert error, and an unguarded .toFixed can take down /scores.

=== NEXT UP ===
[ ] Leaderboard page
    Route + heading exist, nothing else. Click through to other users' scores.

[ ] Play / import history in ScoreModal
    There's no history table — each play is just another scores row. Needs a
    schema decision before it can be built.

=== IMPORT SCORES (Stage 2) ===
[ ] OCR python script
    Candidate: arcaea-offline-ocr on PyPI (KNN + SIFT).
    Extracts score/pure/far/lost/song_id from a screenshot.
    Already speaks the same song_id system used for jackets.

=== STAGE 3 / DEPLOY ===
[x] Replace the guest_id cookie with Supabase signInAnonymously()
    Steps 0-3 of docs/anon_auth_migration.md are done: proxy refreshes,
    anon sign-ins enabled, utils/auth.ts holds getOrCreateUser(), guest.ts
    deleted, schema under supabase/migrations/, ownership policies live.
    Only Step 4 (linkIdentity for the anon -> Google upgrade) remains, and
    nothing is blocked on it.
[x] Version-control the Supabase schema
    supabase/ is committed: db pull baseline + add_scores_rls migration +
    seed.sql (the charts backup, which db reset also loads locally).
    CLI is a devDependency -> run everything as `npx supabase ...`.
[x] UPDATE / DELETE RLS policies on scores
    Four ownership policies on scores, grants narrowed. The DB no longer
    blocks edit/delete — see NEXT UP for the UI.

[X] VERIFY the policies in the app (do this before building on them)
    1. Private window -> /scores must be EMPTY, not show your scores.
    2. /browse must still load with no session (tests the anon grant on
       charts). 42501 = missing grant; empty result = policy denial.
    3. Add a SECOND score as an anon user and confirm auth.users does not
       grow a new row — proves getOrCreateUser reuses the session.

[x] Step 4 — linkIdentity (anon -> Google upgrade)
    /auth/link offers two paths: link (keeps the scores) or sign in to an
    existing account (loses them). NavBar 3-way branch sends anon users there.
    No merge — ImportFromBrowser deleted; RLS had already made the copy
    impossible, and a merge would need proof of ownership to not be theft.

[x] Bug 6 — auth/callback/route.ts
    Error params read before `code` with early returns, so no failure reaches
    the success redirect. identity_already_exists -> /auth/link?error=
    account_exists. Redirect base built from x-forwarded-host/-proto.
    NIT: `${forwardedProto}://${forwardedHost}` gives "undefined://host" if
    the host header arrives without the proto. Use forwardedProto ?? 'https'.

[ ] Surface query errors on /scores
    Neither query destructures error, so a denied policy renders as an
    empty B50 with no error anywhere. This was cosmetic before Step 3 and
    is now the main debugging hazard. report_todo 4.3.

[ ] Public deployment
    Full checklist: docs/anon_auth_migration.md § Deployment.
    NOTE: auth/callback/route.ts builds its redirect from new URL(request.url),
    which behind a proxy is the internal origin — this will redirect to
    localhost in production. Read x-forwarded-host instead. Same file also
    treats a cancelled login as success, and /auth/auth-code-error is empty.
    Deploy to a preview URL first — these only manifest behind a proxy.

=== MAYBE / LOW PRIORITY ===
[ ] Generated Supabase types (supabase gen types typescript)
    ScoreWithChart types the components, but the query still returns `any`.
    Root cause of several report_todo.md findings — higher value than it looks.
[ ] Consolidate the inline Supabase clients in auth/callback/route.ts and
    LoginButton.tsx onto utils/supabase/{server,client}.ts
[ ] Add next/prev buttons in Score/Browse Modal to go to next chart w/o clicking off
